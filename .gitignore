# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
.env
.env.local
.env.*.local

# Vercel CLI — nunca commitar credenciais/project.json
.vercel


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.vercel
playwright-report
test-results

# Stitch design system reference assets
stitch_mycareer_premium_ai_copilot/
stitch_mycareer_premium_ai_copilot.zip
scratch/

.env*

# scripts e evidências de diagnóstico/investigação temporários, não versionados
scratch_*.cjs
scratch_*.png
scratch_*.mjs

# Android — artefatos de build intermediários (não versionar)
android/build/
android/app/build/
android/.gradle/
android/local.properties

# APKs de build intermediário (não versionar) — exceto o publicado em public/
android/**/*.apk
# O APK publicado para download é mantido no repositório:
!public/vocentro-*.apk

# SEGURANÇA: Keystore de produção e credenciais — NUNCA commitar
android/**/*.jks
android/**/*.keystore
android/keystore.properties

# Artefatos locais e scripts de validação temporários
artifacts/
scripts/audit_phase5_browser_validation.mjs
